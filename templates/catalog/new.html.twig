{% extends 'base.html.twig' %}

{% block title %}New Catalog{% endblock %}

{% block body %}
    <h1>Créer catalogue</h1>

    {{ include('catalog/_form.html.twig') }}
    <br>
    <a href="{{ path('catalog_index') }}" class="btn btn-primary">Retour à la liste</a>
{% endblock %}
